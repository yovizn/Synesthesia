export type TransactionsType = {
  id: string
  invoiceNumber: string
  voucherId: null
  eventId: string
  userId: string
  total: string
  discountPoint: string
  createdAt: string
  updatedAt: string
  status: string
  event: { title: string; poster: { name: string } }
}
